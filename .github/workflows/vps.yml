name: Linux-VPS
on: 
  workflow_dispatch:
defaults:
  run:
    shell: bash
    
    
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
#    - name: If failed, means ok
#      run: ./not-exist-file.sh it bloke build
    - name: Preparando el sistema linux.
      run: sudo apt-get update
    - name: Instalando el sistema linux.
    #  if: ${{ failure() }}
      run: bash linux-ssh.sh
      env:
        LINUX_USER_PASSWORD: root
        NGROK_AUTH_TOKEN: 2TnCPH653vAeAgcV1gEdpwIvKpv_6uBdMQPEG4NnQ5eFnWawh
        CHROME_HEADLESS_CODE: 60
        LINUX_MACHINE_NAME: linux
        LINUX_USERNAME: user
        GIT_NAME: SilentKiller345
        GIT_EMAIL: vanshkumar996k@gmail.com
        
    - name: Configurar github.
    # if ${{ failure() }}
      run: git config --global user.email "${{ secrets.GIT_EMAIL }}" && git config --global user.name "${{ secrets.GIT_NAME }}"
           
    - name: Seguir corriendo el sistema linux y dejalo por 6 horas.
     # if: ${{ failure() }}
      run: sleep 6h
